# 🚀 김지원 실내건축 포트폴리오 깃허브(GitHub Pages) 배포 완벽 매뉴얼

> 본 문서는 **김지원** 학생의 실내건축 포트폴리오를 깃허브 저장소(GitHub Repository)에 업로드하고, **무료 정적 웹사이트(GitHub Pages)**로 배포하여 채용 담당자에게 보낼 주소를 생성하는 상세 매뉴얼입니다.

---

## 📋 01. 깃허브 업로드 대상 파일 체크리스트

내 컴퓨터의 포트폴리오 폴더에서 다음 파일들을 준비해 주세요:

- [x] **`index.html`** (포트폴리오 메인 웹 애플리케이션 - 100% 필수)
- [x] **`README.md`** (깃허브 저장소 메인 대문 안내 문서)
- [x] **`.gitignore`** (불필요 임시파일 업로드 제외 설정)
- [x] **`assets/`** (전체 폴더)
  - `assets/cad/` (2D CAD 평면도·천장도·입면도·상세도 SVG/PNG)
  - `assets/renders/` (3D 실시간 하이퍼리얼 렌더 투시도 이미지)
  - `assets/user_drawings/` (내 완성 작품 직접 반영 이미지)

---

## 💻 02. 깃허브 배포 4단계 순서 (초보자 맞춤)

### 1단계: GitHub 로그인 및 저장소(Repository) 생성
1. 인터넷 브라우저를 열고 [https://github.com](https://github.com) 에 접속하여 로그인합니다.
2. 오른쪽 상단의 **`+`** 아이콘을 누르고 **`New repository`**를 선택합니다.
3. **Repository name** 입력란에 **`portfolio`** (또는 `interior-portfolio`)를 입력합니다.
4. 아래 공개 범위 설정에서 **`Public`**을 선택합니다.
5. 최하단의 **`Create repository`** 초록색 버튼을 클릭합니다.

---

### 2단계: 파일 드래그 앤 드롭 업로드
1. 생성된 새 저장소 화면 중앙에 있는 **`uploading an existing file`** 파란색 글씨를 클릭합니다.
2. 내 컴퓨터의 포트폴리오 폴더에 있는 **`index.html`**, **`README.md`**, **`.gitignore`** 파일과 **`assets`** 폴더 전체를 드래그하여 브라우저 박스 안으로 올려놓습니다.
3. 업로드가 모두 완료(파일 목록이 보임)되면, 하단의 **`Commit changes`** 초록색 버튼을 누릅니다.

---

### 3단계: GitHub Pages 무료 웹 호스팅 켜기
1. 저장소 상단 메뉴 바에서 **`Settings`** (톱니바퀴 아이콘)을 클릭합니다.
2. 좌측 메뉴에서 **`Pages`** 탭을 클릭합니다.
3. **Build and deployment** 구역의 **Branch** 옵션을 `None`에서 **`main`** (또는 `master`)으로 변경합니다.
4. 오른쪽 폴더 선택은 **`/ (root)`** 상태 그대로 둔 후 **`Save`** 버튼을 누릅니다.

---

### 4단계: 내 포트폴리오 주소 생성 확인 & 제출
1. 저장소 상단 메뉴에서 `Settings` → `Pages` 페이지를 1~2분 뒤 새로고침합니다.
2. 화면 상단에 다음과 같은 파란색 안내 띠가 등장합니다:  
   > 🟢 **Your site is live at `https://사용자계정.github.io/portfolio/`**
3. 이 주소가 바로 면접관과 채용 사이트에 제출할 **나만의 실내건축 웹 포트폴리오 주소**입니다!

---

## 🔑 03. 학생 전용 편집 및 내용 업데이트 가이드

### 브라우저에서 실시간 편집하기
1. 완성된 내 포트폴리오 주소(`https://사용자계정.github.io/portfolio/`)로 접속합니다.
2. 웹사이트 상단 **`PORTFOLIO` 로고를 연속 3회 클릭**하거나, 주소창 뒤에 **`?admin=1234`**를 붙입니다.
3. 관리자 비밀번호 입력 팝업창에 기본 PIN **`1234`**를 입력합니다.
4. 화면 우측 하단에 생성된 `[ ✏️ 이력서·자소서 편집 ]`, `[ 🏠 대문 문구 수정 ]`, `[ ⚙️ 도면 추가/제외 ]` 버튼으로 브라우저에서 직접 내용을 수정합니다.

### 수정본 깃허브에 반영하기
1. 브라우저 우측 하단 floating 버튼의 **`[ 💾 수정본 영구 저장 ]`** 버튼을 클릭합니다.
2. 다운로드된 최신 **`index.html`** 파일로 깃허브 저장소의 기존 `index.html`을 덮어쓰기(Commit) 업로드합니다.
3. 약 1분 후 배포된 웹사이트 주소에 최신 수정 내용이 영구적으로 적용됩니다.

---

## ❓ FAQ & 문제 해결

- **Q: 이미지나 도면이 엑박(깨짐)으로 보여요!**  
  - A: 깃허브에 `assets` 폴더가 제대로 업로드되었는지 확인해 주세요. 폴더 구조(`assets/cad/`, `assets/renders/`)가 소스코드와 동일해야 합니다.
- **Q: 모바일 스마트폰에서도 잘 보이나요?**  
  - A: 네, 360~390px 스마트폰 환경에 맞춘 글자 겹침 방지 레이아웃과 2D/3D 연속 뷰어가 탑재되어 있습니다.

---

© 2026 Kim Ji-won. All rights reserved. Designed for Interior Architecture Portfolio.
